spring:
  application:
    name: nutaxi-route-service
  profiles:
    active: development

---

spring:
  profiles: development
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/microservicesteam/nutaxi-configs.git